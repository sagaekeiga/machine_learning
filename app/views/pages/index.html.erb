
<% @mashines.each do |tweet| %>
   Date:<%= tweet.date %><br>
   <%= "Tweet:" + tweet.tweet %><br>
   <%= "Nama:" + tweet.name %><br>
   <%= "Text:" + tweet.text %><br>
   Geo:<%= tweet.geo %><br>
   <%= image_tag(tweet.url) %><br><br>
<% end %>

<%= form_for(@word,  html: {class: 'ui form'}) do |f| %>
  <div class="field">
    <label>Word1</label>
      <%= f.text_field :word1, class: '' %>
  </div>
  
  <div class="field">
    <label>Word2</label>
      <%= f.text_field :word2, class: '' %>
  </div>
  
  <div class="field">
    <label>Word3</label>
      <%= f.text_field :word3, class: '' %>
  </div>
  
  <div class="actions">
    <%= f.submit "登録", class: 'ui button'%>
  </div>
<% end %>